// n8n discovers nodes and credentials through the `n8n` field in package.json,
// so this file intentionally exports nothing. It exists only to give the
// package a `main` entry point for tooling that expects one.
export {};
